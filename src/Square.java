package edu.upc.eetac.dsa;

public class Square extends Rectangle {//Hereda de rectangulo, por lo tanto debe contener los mismo atributos que su padre

    public Square(double l1, double l2) {

        super(l1, l2);
    }

    @Override
    public double area() {

        return super.area();
    }
}